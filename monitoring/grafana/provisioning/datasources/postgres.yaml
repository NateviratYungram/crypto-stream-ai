apiVersion: 1
deleteDatasources:
  - name: PostgreSQL
    orgId: 1
datasources:
  - name: PostgreSQL
    uid: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: user
    secureJsonData:
      password: password
    jsonData:
      database: crypto_stream_db
      sslmode: disable
      postgresVersion: 15
    isDefault: false
    editable: true
